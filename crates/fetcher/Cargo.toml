[package]
name = "soltax-fetcher"
version = "0.1.0"
edition = "2024"

[dependencies]
soltax-common = { path = "../common" }
reqwest = { version = "0.12", features = ["json"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
dotenvy = "0.15"
